Legacy in-repo `data/` is no longer used for staged inputs.

Place staged local or HPC data under `PTM_data/` and configure runtime paths through
[`hpc/ptm_env.sh`](/mnt/m/Codex/PTM_pipeline/hpc/ptm_env.sh).
